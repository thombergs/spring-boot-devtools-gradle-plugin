dependencies {
    implementation 'org.springframework.boot:spring-boot-starter-web'
}

task customProcessResources {
    dependsOn('processResources')
}

task customCompileJava {
    dependsOn('compileJava')
}
